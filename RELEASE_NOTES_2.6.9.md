# InvictaTill Browser 2.6.9

## Changes

### Screen Picker — Audio Checkbox Removed
Removed the "Also share audio" checkbox from the screen-share picker footer. The related HTML elements (`#chk-share-audio`, `#screen-picker-audio-label`), CSS rules (`.screen-picker-audio-row`), and JavaScript `els` collection entries have all been cleaned up. The screen picker footer layout is restored to its original single-row style.
