# InvictaTill Browser 2.7.8

## Bug Fixes

- **HTTP Basic Auth Layout**: Fixed an issue where the HTTP Basic Auth modal was overlapping with the main browser UI instead of correctly staying clipped within the active tab viewport. The prompt is now properly positioned relative to the browser chrome and app rail.
- **Download Popout Position**: Fixed a subpixel rendering overlap between the download flyout and the browser chrome.
